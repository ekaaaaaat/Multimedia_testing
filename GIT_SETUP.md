# Настройка Git и автоматических коммитов

## ✅ Что уже настроено:

1. **Git репозиторий инициализирован**
2. **Удаленный репозиторий подключен**: https://github.com/ekaaaaaat/Multimedia_testing.git
3. **Автоматические Git hooks настроены**

## 🔄 Автоматические коммиты

### Способ 1: Использование скрипта (рекомендуется)

После внесения изменений в код, выполните:

**На Windows (PowerShell):**
```powershell
.\auto-commit.ps1 "Ваше сообщение коммита"
```

**Или с автоматическим сообщением:**
```powershell
.\auto-commit.ps1
```

**На Linux/Mac или через Node.js:**
```bash
npm run commit "Ваше сообщение коммита"
```

### Способ 2: Git hooks (автоматически)

Git hooks уже настроены и будут работать автоматически:

- **pre-commit**: Автоматически добавляет все изменения перед коммитом
- **post-commit**: Автоматически отправляет изменения в репозиторий после коммита

Просто используйте стандартные команды Git:
```bash
git commit -m "Ваше сообщение"
# Изменения автоматически отправятся в репозиторий
```

## 📝 Ручной способ

Если хотите сделать коммит вручную:

```bash
git add .
git commit -m "Ваше сообщение коммита"
git push origin main
```

## ⚙️ Настройка Git (если нужно)

Если это первый раз использования Git на этом компьютере, настройте имя и email:

```bash
git config --global user.name "Ваше имя"
git config --global user.email "ваш@email.com"
```

## 🔍 Проверка статуса

Проверить статус репозитория:
```bash
git status
```

Проверить подключенные удаленные репозитории:
```bash
git remote -v
```

## 📤 Отправка изменений

Если hooks не сработали автоматически:
```bash
git push origin main
```





